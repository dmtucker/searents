image: python:3
test:
  script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - ./setup.py sdist
    - pip install dist/*
    - searents --help
    - searents-parse --help
    - pip install pep8 pylint
    - pep8 searents setup.py
    - pylint searents setup.py
